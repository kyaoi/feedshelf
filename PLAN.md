# PLAN

## 目的

FeedShelf の v1 を、仕様先行・最小差分・GitHub Pages 前提で安全に実装する。

## 前提

- 記事本文は保持しない
- 記事閲覧は外部遷移とする
- オフライン対応は v1 の対象外
- 入力は公開 RSS / Atom のみとする
- 配信は GitHub Pages、更新は GitHub Actions を前提とする

## 進め方

- まず仕様と計画を固める
- 次にデータ契約と取得フローを実装する
- その後に UI と更新基盤を最小構成で追加する
- 各ステップはできるだけ 1 タスク = 1 コミットで進める

## フェーズ

### Phase 0: 仕様基盤

- [x] `FS-SPEC-01` spec docs を作成する
- [x] `FS-SPEC-02` decisions / traceability を拡張する
- [ ] `FS-SPEC-03` 実装計画 `PLAN.md` を追加する

### Phase 1: データ契約の具体化

- [ ] `FS-DATA-01` フィード定義ファイルの置き場とスキーマを決める
- [ ] `FS-DATA-02` 記事正規化オブジェクトの最終形を決める
- [ ] `FS-DATA-03` URL 正規化と dedupe ルールを具体化する
- [ ] `FS-DATA-04` 生成物 JSON の分割単位を決める

完了条件:
- 実装時に迷わないレベルで `data/` と JSON 生成物の契約が決まっている

### Phase 2: 取得・生成パイプライン

- [ ] `FS-PIPE-01` フィード取得スクリプトを追加する
- [ ] `FS-PIPE-02` RSS / Atom の正規化処理を追加する
- [ ] `FS-PIPE-03` 重複排除処理を追加する
- [ ] `FS-PIPE-04` 一覧表示用 JSON を生成できるようにする

完了条件:
- 手元実行でフィード取得から JSON 生成まで通る

### Phase 3: Web UI 最小実装

- [ ] `FS-WEB-01` トップ / 新着一覧ページを作る
- [ ] `FS-WEB-02` カテゴリ別一覧ページを作る
- [ ] `FS-WEB-03` 媒体別一覧ページを作る
- [ ] `FS-WEB-04` 記事カードから外部遷移できるようにする

完了条件:
- 新着 / カテゴリ / 媒体の 3 導線で記事を閲覧できる

### Phase 4: 更新・公開基盤

- [ ] `FS-OPS-01` GitHub Actions の定期更新 workflow を追加する
- [ ] `FS-OPS-02` GitHub Pages 公開フローを追加する
- [ ] `FS-OPS-03` 単一フィード失敗時の継続方針を実装へ反映する

完了条件:
- 定期更新と公開が GitHub 上で自動化されている

### Phase 5: MVP 仕上げ

- [ ] `FS-QA-01` 仕様と実装の差分を確認する
- [ ] `FS-QA-02` README / docs の導線を整える
- [ ] `FS-QA-03` MVP 受け入れ条件を満たすか確認する

完了条件:
- `docs/SPEC_V1.md` の MVP / 受け入れ条件を満たしている

## 直近の次タスク

1. `FS-DATA-01`: フィード定義ファイルの置き場とスキーマを決める
2. `FS-DATA-02`: 記事正規化オブジェクトの最終形を決める
3. `FS-PIPE-01`: フィード取得スクリプトの最小構成を追加する

## メモ

- まずは docs と実装のズレを増やさないことを優先する
- 仕様変更が入ったら `SPEC_V1` / `DECISIONS` / `TRACEABILITY` / `PLAN` のどこを更新するかを明確にする
- 本文取得やオフライン対応は v1 完了後の別フェーズで検討する
